# Zerodha Algo Trader - Environment Configuration
# Copy this file to .env and update with your actual values
#
# SECURITY: This file contains sensitive credentials.
# NEVER commit .env to version control (it's in .gitignore)

# ============================================================================
# Environment (development/production)
# ============================================================================
ENV=development
LOG_LEVEL=INFO

# ============================================================================
# Application Ports
# ============================================================================
API_PORT=8000
DASHBOARD_PORT=8501

# ============================================================================
# Database (PostgreSQL/TimescaleDB)
# ============================================================================
# REQUIRED in production - connection string format:
# postgresql://username:password@host:port/database
DATABASE_URL=postgresql://trader:CHANGE_THIS_PASSWORD@localhost:5432/algotrader

# Individual components (for Docker)
POSTGRES_DB=algotrader
POSTGRES_USER=trader
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_PORT=5432

# ============================================================================
# Redis (Caching & State)
# ============================================================================
# REQUIRED in production - connection string format:
# redis://:password@host:port/db
REDIS_URL=redis://:CHANGE_THIS_PASSWORD@localhost:6379/0

REDIS_PASSWORD=CHANGE_THIS_PASSWORD
REDIS_PORT=6379

# ============================================================================
# Zerodha API Credentials
# ============================================================================
# REQUIRED in production - get from https://developers.kite.trade/
ZERODHA_API_KEY=your_api_key_here
ZERODHA_API_SECRET=your_api_secret_here
# Access token is generated daily - see scripts/generate_token.py
ZERODHA_ACCESS_TOKEN=

# ============================================================================
# Optional Tools
# ============================================================================
# PgAdmin
PGADMIN_EMAIL=admin@algotrader.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Redis Commander
REDIS_COMMANDER_PORT=8081

# ============================================================================
# Notes:
# ============================================================================
# 1. NEVER commit .env file to version control
# 2. Change all default passwords before production use
# 3. Use docker-compose --profile tools up to start optional tools
